1. git init
inicia git, solo se hace la primera vez
2. git status
para check que todo este bien o esté todo mal
3. git add .
esto permite agregar las modificiaciones  que hayamos hecho
4. git commit -m "un mensaje cualquiera"
5. git log 
control de versiones

sass --watch scss:css
